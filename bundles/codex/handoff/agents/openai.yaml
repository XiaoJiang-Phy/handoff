interface:
  display_name: "Handoff"
  short_description: "Transfer or resume work across agent sessions"
  default_prompt: "Use $handoff to prepare or resume a verified cross-platform session handoff."
policy:
  allow_implicit_invocation: false
